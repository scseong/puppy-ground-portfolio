'use client';

import styles from './customSelect.module.scss';

const obj = [];

const CustomSelect = (list: string[]) => {
  console.log(list);

  return <div>hihi</div>;
};

export default CustomSelect;
